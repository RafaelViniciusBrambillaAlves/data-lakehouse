# Built-in TPC-H connector — handy for smoke-testing Trino connectivity
# without any dependency on MinIO or the Hive Metastore.
# Example: SELECT * FROM tpch.tiny.nation LIMIT 5;
connector.name=tpch
